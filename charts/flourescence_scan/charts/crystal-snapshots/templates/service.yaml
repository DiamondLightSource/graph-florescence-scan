apiVersion: v1
kind: Service
metadata:
  name: {{ include "fluorescence_scan.fullname" . }}
  labels:
    {{- include "fluorescence_scan.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fluorescence_scan.selectorLabels" . | nindent 4 }}
